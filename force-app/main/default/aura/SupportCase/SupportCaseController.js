({
    onInit: function(cmp, event, helper) {
        helper.doInit(cmp, event);
    },
    onSubmit: function(cmp, event, helper)
    {
        helper.doSubmit(cmp, event);
    },
    onSuccess: function(cmp, event, helper)
    {
        helper.doSuccess(cmp,event);
    }
})
